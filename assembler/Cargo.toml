[package]
name = "assembler"
version = "0.1.0"
edition = "2024"

# The assembler is a library (the pipeline + ISA tables) with a thin CLI on top.
[lib]
name = "asm"
path = "src/lib.rs"

[[bin]]
name = "asm"
path = "src/main.rs"

[dependencies]
